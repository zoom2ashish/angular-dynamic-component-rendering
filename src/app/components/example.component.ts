import { Component } from "@angular/core";

@Component({
    selector: 'app-example',
    template: `I am from angular example component rendered dynamically from angular`
})
export class ExampleComponent {
}